name: agent37_flutter
description: A new Flutter project.

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  flutter_cupertino_localizations: ^1.0.1
  cupertino_icons: ^0.1.2
  dio: ^3.0.8
  gallery_saver: ^2.0.1
  keyboard_actions: ^3.2.0
  fluro: ^1.5.2
  provider: ^3.1.0
  shared_preferences: ^0.5.6+1
  flutter_screenutil: ^1.0.2
  color_dart: ^0.2.1
  fluttertoast: ^3.1.3
  url_launcher: ^5.4.5
  get_it: ^3.0.1
  flutter_easyrefresh: ^2.0.8
  extended_nested_scroll_view: ^0.4.0
  flutter_html: ^0.11.1
  city_pickers: ^0.1.30
  event_bus: ^1.1.1
  flutter_datetime_picker: ^1.3.5
  qr_flutter: ^3.2.0
  date_format: ^1.0.8
  flutter_swiper: ^1.1.6
  image_picker: ^0.6.6+1
  path_provider: ^1.6.8
  image_picker_saver: ^0.3.0
  flutter_picker: ^1.1.3
  flutter_alipay: ^1.0.0
  # sy_flutter_alipay: ^0.2.1
  # sy_flutter_wechat: ^0.2.2
  fluwx: ^2.0.7
  # flutter_easyloading: ^1.1.4
  flutter_spinkit: ^4.1.2
  simple_tooltip: ^0.1.3
  shape_of_view: ^1.0.2
  flutter_custom_dialog: ^1.0.19
  badges: ^1.1.1
  cached_network_image: ^2.1.0+1
  # flutter_ijkplayer: 0.3.5+1
  video_player: 0.10.9
  chewie: 0.9.10
  auto_orientation: 1.0.6
  webview_flutter: 0.3.20+2
  flutter_webview_plugin: ^0.3.11
  transparent_image: ^1.0.0
  permission_handler: ^5.0.0+hotfix.5
  # webview_flutter: ^0.3.19+9 #flutter官方webview
dev_dependencies:
  flutter_test:
    sdk: flutter


# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
  - lib/assets/images/
  - lib/assets/images/enterprise/
  - lib/assets/images/common/
  - lib/assets/images/order-result/
  - lib/assets/images/pic-icon/
  - lib/assets/images/vip/
  - lib/assets/images/home/
  - lib/assets/images/wallet/
  - lib/assets/images/marketing/
  fonts:
    - family: iconfont
      fonts:
        - asset: lib/assets/fonts/iconfont.ttf
