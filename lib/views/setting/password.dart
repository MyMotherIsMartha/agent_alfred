import 'package:flutter/material.dart';

class SettringPwdPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('设置登录密码'),
      ),
    );
  }
}